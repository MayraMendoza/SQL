The INNER JOIN keyword selects records that have matching values in both tables.
Practice 
SELECT * FROM Orders Inner Join Customers ON orders.CustomerId=Customer.CustomerID

African Cities -https://www.hackerrank.com/challenges/african-cities/problem?isFullScreen=true
Select city.NAME from city Inner join country on city.COUNTRYCODE = country.CODE WHERE country.CONTINENT ='Africa';