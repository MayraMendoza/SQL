The select statement is used to select data from a database.

Example
SELECT column1, column2
FROM table_name;

Here column1, column 2 are the field names of the table you want to select data from.

If you want to select all fields available in the table, use:
SELECT * FROM table_name;
